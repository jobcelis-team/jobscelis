# ============================================
# JOBSCELIS - Docker Compose
# Desarrollo: app + PostgreSQL. Cargar .env
# ============================================

services:
  # ============================================
  # APLICACIÓN
  # ============================================

  jobscelis:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: jobscelis
    env_file:
      - .env
    environment:
      - MIX_ENV=${MIX_ENV:-dev}
      - PORT=4000
      - PHX_HOST=${PHX_HOST:-localhost}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - GUARDIAN_SECRET_KEY=${GUARDIAN_SECRET_KEY}
      - LIVE_VIEW_SIGNING_SALT=${LIVE_VIEW_SIGNING_SALT}
      - DATABASE_URL=ecto://${DB_USERNAME:-postgres}:${DB_PASSWORD:-postgres}@db:5432/${DB_DATABASE:-jobscelis_dev}
      - LOG_LEVEL=${LOG_LEVEL:-debug}
    ports:
      - "${PORT:-4000}:4000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
      - deps:/app/deps
      - build:/app/_build
      - node_modules:/app/apps/streamflix_web/assets/node_modules
    networks:
      - jobscelis_network
    stdin_open: true
    tty: true

  # ============================================
  # BASE DE DATOS
  # ============================================

  db:
    image: postgres:17-alpine
    container_name: jobscelis_db
    environment:
      - POSTGRES_USER=${DB_USERNAME:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
      - POSTGRES_DB=${DB_DATABASE:-jobscelis_dev}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - jobscelis_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

# ============================================
# VOLUMES
# ============================================

volumes:
  postgres_data:
  deps:
  build:
  node_modules:

# ============================================
# RED
# ============================================

networks:
  jobscelis_network:
    driver: bridge
